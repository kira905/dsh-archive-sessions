# 统一换行：仓库内一律 LF（跨平台 clone 不产生整文件 diff）
* text=auto eol=lf

# Windows 专用脚本（若有）保留 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制，不做换行转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
